# Phase 1 Issue Register

Status values distinguish implementation from validation. `Simulated verified` never means staging verified.

| ID | Severity | Component | Evidence | Root cause | Intended behavior | Proposed fix | Implementation | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| CC5-001 | High | Shell installer | `build/5.0/install-issabel-callcenter.sh:63-200` stages source, module, dialer/service, database, and health boundaries through lifecycle guards | Required stages are checked and terminate on failure | Any required failure returns nonzero and suppresses completion | Add strict staged execution, preflight, and post-install health checks | Implemented | Simulated verified; authenticated upgrade/repeat and fixed clean installation staging verified through `29adf38` |
| CC5-002 | High | Temporary cleanup | `build/5.0/install-issabel-callcenter.sh` creates registered `mktemp` checkout/module paths and cleans them with an exit trap | Temporary resources are unique to a run and registered | Cleanup removes only exact temporary paths created by the current process | Use `mktemp`, a validated registry, and an exit trap | Implemented | Simulated verified |
| CC5-003 | High | Asterisk reload | `build/5.0/install-issabel-callcenter.sh` calls `asterisk -rx 'core reload'` through a required lifecycle stage | Reload arguments are separate and failure propagates | Pass two arguments and require a successful reload | Use `asterisk -rx 'core reload'` inside the required-stage wrapper | Implemented | Simulated verified |
| CC5-004 | High | PHP installer | `setup/installer_lib.php` and `setup/installer.php` now guard required database/configuration operations and reject unavailable or malformed Asterisk output | Helper functions returned without propagating failure; Asterisk parsing defaulted to 18 | Required database/configuration failures terminate with exit `1` | Add result guards, exceptions, checked file writes, and strict Asterisk parsing | Implemented | Portable Windows PHP 7.4 unit/syntax plus authenticated PHP 7.4 upgrade, repeat-repair, and clean-install staging evidence |
| CC5-005 | High | Removal | `build/5.0/remove-issabel-callcenter.sh:176-297` stops/disables the service, removes exact targets, handles explicit `call_center` database modes, and checks postconditions | Removal previously had no explicit stage contract or verified postconditions | Keep/delete DB modes are explicit; required failures stop; exact targets are verified | Share lifecycle guards, add explicit flags and allowlisted postconditions | Implemented | Simulated verified; keep/delete database removal and exact-commit clean reinstallation independently staging verified in E-P1-010 |
| CC5-006 | High | Compatibility | Authenticated fresh clone at `751a62f`; secret-safe baseline and two `--local` installer runs recorded in E-P1-009 | Previous screenshot/unauthenticated evidence lacked runtime authority | Compatibility matrix contains authenticated clone versions and evidence levels | Run secret-safe baseline collector and upgrade/repeat validation through verified SSH access | Completed | Staging verified for observed Rocky 8.8, kernel 4.18.0-477.27.1.el8_8, PHP 7.4.33, MariaDB 10.3.39, Asterisk 18.19.0, Apache 2.4.37, and systemd 239; installer release string `5.0.0-1` from `751a62f` completed twice, not an independently queried package/module version |
| CC5-007 | Medium | Runtime architecture | `CampaignProcess.class.php:146-150,377`; `AMIEventProcess.class.php:260,305-324`; `SQLWorkerProcess.class.php:763-878`; `ECCPWorkerProcess.class.php:125,225` show competing memory, DB, and AMI state paths | Authoritative-state and reconciliation rules are implicit in large worker classes | Document process ownership, persistence, AMI/AGI boundaries, and hypotheses | Maintain `docs/architecture.md` and resolve hypotheses in later focused phases | Open | Static evidence only |
| CC5-008 | Medium | Local PHP container | Docker engine is unavailable (`docker version` exit `1`) | Docker Desktop engine pipe is absent | Run PHP 7.4 checks in staging before mutation | Perform Task 8 staging PHP 7.4 checks | Completed | Authenticated clone PHP 7.4.33 syntax and upgrade/repeat validation verified; local Docker remains unavailable |
| CC5-009 | High | Canonical campaign schema | Clean install at `fa993c9` returned MariaDB `1054` for `campaign.id_url2` and equivalent fields; runtime campaign code queries all four fields | Vendor page-load migrations added URL2/URL3 lazily, but canonical SQL and the installer repair path omitted them | Clean install and repeat upgrade create the four nullable unsigned fields and foreign keys before runtime pages execute | Add canonical SQL, centralized installer repair, and exact schema/repair regressions in `29adf38` | Implemented | Native tests, repeat repair, delete/reinstall, semantic schema equality, runtime projections, and independent staging verification passed |
| CC5-010 | Medium | Incoming campaign creation | `modules/campaign_in/index.php:419-445` passes URL1/2/3; pre-fix PHP 7.4 test bound URL1 as `11` but URL2/URL3 as `NULL`, and the controller converted tampered `12junk` input to valid ID `12` | Incoming creation accepted only URL1; lossy controller casts preceded class validation; update omitted URL2/URL3 validation and leaked SQL through `var_dump` | Incoming campaigns preserve and validate all selected external URLs without debug output | Preserve raw form values for class validation, add optional URL2/URL3 parameters, validate all URL IDs before database access, remove debug output, and add campaign-workflow tests | Implemented | Portable and native staging PHP 7.4 tests passed; rollback-isolated create/read/update/read/delete cycle and independent zero-residue/zero-call verification passed at `e2e2ba3` |
| CC5-011 | High | Incoming page schema migration | Pre-fix `modules/campaign_in/index.php:44` called `checkDataBase`; the incoming class read the root password and shelled out to grant/revoke `ALTER` during a page request | Historical lazy URL2/URL3 migration remained after the canonical installer/schema repair | Incoming requests never mutate schema or expose database credentials in process arguments; the installer owns migrations | Remove the page-load call and privileged helpers; execute the real request bootstrap against deterministic external boundaries | Implemented | Portable PHP 7.4 RED/GREEN passed; native PHP 7.4 focused and installed-source checks passed at exact staging commit `2d291bb`, with stable schema/grant fingerprints, zero-use health, and independent verification |
| CC5-012 | High | Outgoing page schema migration | Pre-fix `modules/campaign_out/index.php:61` called `checkDataBase`; the outgoing class read the root password and shelled out to grant/revoke `ALTER` during a page request | The outgoing copy of the historical lazy URL2/URL3 migration was independent of the removed incoming copy | Outgoing requests never mutate schema or expose database credentials in process arguments; the installer owns migrations | Apply a separate executable request-bootstrap regression, then remove the outgoing call and helpers | Implemented | Portable PHP 7.4 RED/GREEN and native staging checks passed at exact commit `8e50dbf`; immutable installed hashes, complete schema/privilege fingerprints, zero-use health, and independent verification passed |
| CC5-013 | Medium | Campaign-monitoring database configuration | The shipped client calls `modules/campaign_monitoring/libs/api.php`; pre-fix staging reached it with HTTP `200`, while the endpoint parsed `/etc/amportal.conf` and assigned database user/password from `AMPDBNAME` instead of the existing module DSN | A standalone endpoint retained a duplicate, incorrect configuration parser outside the module's authoritative configuration path | The endpoint initializes its existing `mysqli` boundary from `campaign_monitoring/configs/default.conf.php` and never reads `/etc/amportal.conf` | Replace only the bootstrap parser with the module DSN and execute the real endpoint against a deterministic `mysqli` boundary | Implemented | Portable PHP 7.4 RED/GREEN and native PHP 7.4 GREEN passed at exact commit `1ae2948`; exact installed hashes, installed behavioral probe, parameter-free HTTP `200`/zero-byte response, stable fingerprints/zero-use health, and independent staging verification passed |
| CC5-014 | High | Campaign-monitoring queue command boundary | `modules/campaign_monitoring/libs/api.php` reads `$_GET['queue']` into an Asterisk shell command at both campaign branches; the endpoint returned HTTP `200` to a parameter-free loopback request without an observed session check | The directly requested endpoint has no explicit authorization/queue validation boundary before constructing a shell command from request data | Only an authorized, validated queue identifier reaches a non-shell Asterisk/AMI boundary; request data is never interpolated into a command string | First capture the client/auth contract and a local executable regression, then require authorization and strict queue allowlisting while replacing shell construction with a non-shell Asterisk/AMI lookup; treat allowlisting as defense in depth and send no malicious live input | Open | High-confidence static source evidence and parameter-free reachability only; no queue value, malicious payload, call, or route mutation was exercised |
